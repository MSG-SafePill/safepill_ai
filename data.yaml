# Example data.yaml.
# For multi-class pill product training, prefer:
#   python split_data.py --images-dir ... --labels-dir ... --output-dir ... --class-names-file ... 
# which auto-generates a dataset-specific data.yaml.

train: C:/AIData/pill_dataset/images/train
val: C:/AIData/pill_dataset/images/val

nc: 1
names:
  - pill